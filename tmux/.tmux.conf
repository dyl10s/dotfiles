set-option -g default-terminal "xterm-256color"

set -ga terminal-overrides ",xterm*:Tc"
set-option -sa terminal-features ',xterm*:RGB'

set -s escape-time 1

unbind C-b
set-option -g prefix C-Space
bind-key C-Space send-prefix
set -g status-style 'bg=#333333 fg=#5eacd3'

bind r source-file ~/.tmux.conf
set -g base-index 1

set-window-option -g mode-keys vi
bind -T copy-mode-vi v send-keys -X begin-selection
bind -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'

set-option -g renumber-windows on
set -g mouse on

bind | split-window -h 
bind _ split-window -v 

# vim-like pane switching
bind -r ^ last-window
bind -r k select-pane -U
bind -r j select-pane -D
bind -r h select-pane -L
bind -r l select-pane -R

# forget the find window.  That is for chumps
bind-key f display-popup -E -w 60% -h 60% "~/custom-scripts/tmux-sessionizer"

# LazyGit
bind-key g display-popup -E -w 90% -h 90% lazygit
 
# LazyDocker
bind-key d display-popup -E -w 90% -h 90% lazydocker

# GH Dash
bind-key H display-popup -E -w 90% -h 90% "gh dash"

# List of plugins
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'catppuccin/tmux'

# Status bar
set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator " "
set -g @catppuccin_window_middle_separator " █"
set -g @catppuccin_window_number_position "right"

set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"

set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W"

set -g @catppuccin_status_modules_right "directory session"
set -g @catppuccin_status_left_separator  " "
set -g @catppuccin_status_right_separator ""
set -g @catppuccin_status_right_separator_inverse "no"
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"

set -g @catppuccin_directory_text "#{pane_current_path}"

# Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'

# Slack unread badge (must come after tpm so catppuccin's status-right is set first)
set -g status-interval 1
set -ag status-right '#[align=centre]#(~/custom-scripts/slack-unread)'

# Control panel (fzf menu of quick actions)
bind-key P display-popup -E -w 40% -h 40% "~/custom-scripts/control-panel"
